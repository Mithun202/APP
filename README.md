# APP

A small, complete Python starter project with:

- A simple command-line app (`main.py`)
- Reusable project logic (`app.py`)
- Unit tests (`tests/test_app.py`)

## Run

```bash
python3 main.py "my project"
```

## Test

```bash
python3 -m unittest discover -s tests -p 'test_*.py' -v
```
